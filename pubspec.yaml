# Copyright 2022 The RotatingButton Author. All rights reserved.
# Use of this source code is governed by a MIT-style license that
# can be found in the LICENSE file.

name: rotating_button
description: Flutter package for rotating and smoothly animating any child widget of its own when tap.
version: 1.0.0
homepage: null

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: '>=2.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: null